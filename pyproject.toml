[project]
name = "modbustest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.15"
dependencies = [
    "pymodbus>=3.13.0",
    "pyserial>=3.5",
]
